spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: alex
    password: alex
    url: jdbc:postgresql://localhost:5432/spring_professional_data

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect